apply plugin: 'application'

sourceCompatibility = 1.8

mainClassName = "com.perihelios.experimental.proc_oo_ast.ExampleUsage"

dependencies {
	compile project(':groovy-ast')
}

compileGroovy {
	// Uncomment for debugging during compilation
	//options.forkOptions.jvmArgs = ["-agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=5005"]
}

run {
	// Uncomment for debugging during execution
	//jvmArgs = ["-agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=5005"]
}
